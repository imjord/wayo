const db = require('./connection') // connect to our mondb db file
const { User, Products } = require('../models'); // import our models that juan boi made

db.once('open', async () => {
    await Category,deleteMant
})